<%= form_with model: [:admin, team] do |form| %>
  <%= form.label :name, "Name: " %>
  <%= form.text_field :name, required: true, autofocus: true, placeholder: "Team name" %>

  <%= form.submit "Save it" %>
<% end %>
