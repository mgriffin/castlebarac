<%- content_for :title, "Login" %>
<section class="content">
  <p>Registering on this site will not make you a member of the club, but it will allow you to see extra information like:</p>
  <ul>
    <li>race results</li>
    <li>coaching resources</li>
    <li>comments on news stories</li>
    <li>and more</li>
  </ul>
  <p>Any new accounts will be restricted until they're verified by a committee member</p>
  <div class="modal-form">
    <h2>Members login</h2>
    <% if @user.errors.any? %>
      <div class="error-message">
        <%= @user.errors.full_messages.to_sentence.capitalize %>
      </div>
    <% end %>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="mb-3">
        <%= f.label :email, "Email Address: ", class: "form-label" %>
        <%= f.email_field :email, required: true, autocomplete: "email", placeholder: "person@example.com", class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= f.label :password, "Password: ", class: "form-label" %>
        <%= f.password_field :password, required: true, autocomplete: "current-password", class: "form-control" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="mb-3">
          <%= f.check_box :remember_me, class: "form-check-input" %>
          <%= f.label :remember_me, class: "form-check-label" %>
        </div>
      <% end %>

      <%= f.submit "Log in", class: "btn btn-primary" %>
    <% end %>
    <p>Don't have an account yet? <%= link_to "Register", new_registration_path(resource_name) %> for one now.</p>
  </div>
</section>
