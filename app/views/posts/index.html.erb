<%- if @posts %>
  <hr>
  <%- @posts.each do |post| %>
    <h2><%= link_to post.title, edit_post_url(post.id) %></h2>
    <p><%= post.content %></p>
    <hr>
  <%- end %>
<%- else %>
  <p>There are no posts</p>
<% end %>
